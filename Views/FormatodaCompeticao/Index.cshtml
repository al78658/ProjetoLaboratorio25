@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8" />
    <title>Formato da Competição</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #808080;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            position: relative;
        }

        .top-left-logo {
            position: absolute;
            top: 40px;
            left: 50px;
        }

            .top-left-logo img {
                width: 160px;
            }

        .main-container {
            margin-top: 80px;
            margin-bottom: 60px;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 60px 20px;
        }

        h2 {
            font-size: 40px;
            color: black;
            margin-bottom: 40px;
        }

        .fase-selector {
            text-align: center;
            margin-bottom: 40px;
        }

            .fase-selector label {
                font-size: 22px;
                margin-right: 10px;
            }

            .fase-selector input {
                width: 60px;
                font-size: 20px;
                padding: 6px;
                text-align: center;
            }

        /* Mostrar setas sempre no input tipo number */
        input[type="number"]::-webkit-inner-spin-button,
        input[type="number"]::-webkit-outer-spin-button {
            opacity: 1 !important;
        }

        table {
            width: 100%;
            max-width: 900px;
            border-collapse: collapse;
            margin-top: 20px;
            font-size: 18px;
            background-color: white; /* fundo branco por dentro */
        }

        thead {
            background-color: #ccc;
        }

        th, td {
            padding: 14px 10px;
            border: 1px solid #333;
            text-align: center;
        }

        tbody td {
            background-color: white;
        }

        select {
            font-size: 16px;
            padding: 8px;
            border-radius: 6px;
        }

        .config-button {
            padding: 10px 18px;
            font-size: 16px;
            border: 1px solid black;
            background: white;
            border-radius: 6px;
            cursor: pointer;
            margin-right: 8px;
        }

        .config-icon {
            font-size: 20px;
            cursor: pointer;
        }

        .next-button {
            margin-top: 50px;
            font-size: 22px;
            padding: 16px 50px;
            border: 2px solid black;
            background: white;
            border-radius: 10px;
            cursor: pointer;
            box-shadow: 3px 3px 6px rgba(0,0,0,0.2);
        }

            .next-button:hover {
                background-color: #dcdcdc;
            }

        .back-button {
            position: absolute;
            right: 40px;
            bottom: 40px;
            padding: 12px 24px;
            font-size: 18px;
            border: 2px solid #000;
            background: white;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            color: black;
        }

            .back-button:hover {
                background-color: #dcdcdc;
            }

        media (max-width: 768px) {
            h2

        {
            font-size: 28px;
        }

        .fase-selector label {
            font-size: 18px;
        }

        .next-button {
            font-size: 18px;
            padding: 14px 36px;
        }

        table {
            font-size: 16px;
        }

        }
    </style>
</head>
<body>

    <!-- Logotipo -->
    <div class="top-left-logo">
        <img src="img/logotipo.png" alt="Logotipo" />
    </div>

    <!-- Conteúdo -->
    <div class="main-container">
        <h2>Formato da Competição</h2>

        <div class="fase-selector">
            <label for="numFases">Número de Fases</label>
            <input type="number" id="numFases" name="numFases" min="1" max="10" value="2" />
        </div>

        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Nome da Fase</th>
                    <th>Tipo da Fase</th>
                    <th>Configuração</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>1</td>
                    <td>Fase 1</td>
                    <td>
                        <select>
                            <option>Escolher Formato</option>
                            <option>Eliminação Simples</option>
                            <option>Grupos</option>
                            <option>Liga</option>
                        </select>
                    </td>
                    <td>
                        <button class="config-button">Configurar</button>
                        <span class="config-icon">⚙️</span>
                    </td>
                </tr>
                <tr>
                    <td>2</td>
                    <td>Fase 2</td>
                    <td>
                        <select>
                            <option>Escolher Formato</option>
                            <option>Eliminação Simples</option>
                            <option>Grupos</option>
                            <option>Liga</option>
                        </select>
                    </td>
                    <td>
                        <button class="config-button">Configurar</button>
                        <span class="config-icon">⚙️</span>
                    </td>
                </tr>
            </tbody>
        </table>

        <button class="next-button">Seguinte</button>
    </div>

    <!-- Botão Voltar -->
    <a href="/" class="back-button">&lt;Voltar</a>

</body>
</html>
